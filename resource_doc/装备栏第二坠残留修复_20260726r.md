# 装备栏第二坠残留修复 — 2026-07-26r

## 结论（先看）

| # | 用户现象 | 根因 | 本轮处理 |
|---|----------|------|----------|
| A | 第二坠仍红底（Tip/选卸已 OK） | `GetSlotXY` **BP23** 原生 `(104,134)` 与第二坠同格，空槽「项链」标签叠在 BP51 上 | 仅 park **`GetSlotXY[22]→(-128,-128)`**；**不改** HitTest[22]；不碰宠物 HT |
| B | 宠物窗被主窗挡住；「宠物装备」贴底边 | **底图并未变高**：MCP 实测 live/V16 `Equip/backgrnd` 均为 **175×304**（= 用户参考 PNG）。变「高」是因为强制 `+0x5E8=1` 后原生按 **展开栏** 加 **`+0x21(33)`**（Y 155→188，宽 175→208） | 保留 flag=1（绘制上界要扫到 index50），但把 `and r,0x21` 三处改成 `and r,0` |
| C | −51 四维像没生效 | 服务端 `recalcEquipStats` **已遍历全部 EQUIPPED（含 −51）**；客户端战斗路径 `cmp -51 @77F879` 走 `jge` 正常槽。红底/无效着色造成「不生效」观感 | 修红底 + `@第二坠` 打印 −51 四维与面板合计；**无需改 EquipSlot** |
| 美术 | 怀疑底图被换成更高版 | orange-wz：live = V16 = 参考图尺寸 **175×304**；`FullBackgrnd` 208×291 | **未** MCP 覆盖 `.img`（无需还原） |

戳记：**`PENDANT2_REMAIN_20260726r`**  
`pendant2_ui=true`（部署保留）

> IDA MCP 仍是 GMS v95 — 本轮 VA 一律以 **Client_1 `BeiDou.exe` PE** 为准。

---

## A. 美术核对（orange-wz MCP）

| 节点 | LIVE Client_1 | V16 | 用户参考 PNG |
|------|---------------|-----|--------------|
| `Equip/backgrnd` | **175×304** | 175×304 | 175×304 |
| `Equip/FullBackgrnd` | 208×291 | 208×291 | — |
| `Equip/pet` | 177×181 | 177×181 | — |

参考图：经典 5×7、**单**项链、骑兽/鞍子底行、空槽蓝灰 — 与当前 `backgrnd` 一致。  
**禁止** robocopy；本轮也 **没有** `copy_nodes` 写 UI（无需要）。

---

## B. 布局补丁（经典窄栏 + flag）

| VA | 原字节 | 后（进图挂载） | 含义 |
|----|--------|----------------|------|
| `0x007FDE9D` | `83 E0 21` | `83 E0 00` | 绘制侧展开增量清零 |
| `0x00801A5F` | `83 E1 21` | `83 E1 00` | 宠物按钮 Y：155+0 |
| `0x00801A77` | `83 E0 21` | `83 E0 00` | 宠物相关宽/X：175+0 |

flag 仍为 1 → `0x007FEFB9` setnz 上界仍为 51；**禁止** ForceDrawLoop。

---

## C. 红底 / 坐标（沿用 q + 本轮）

| 项 | 值 |
|----|-----|
| 第二坠 GetSlotXY/HitTest[50] | `(104,134)` |
| 腰带[49] | `(71,167)` |
| 勋章[48] | `(5,68)` |
| **BP23 GetSlotXY[22]** | **`(-128,-128)`**（本轮） |
| BP23 HitTest[22] | 保持原生 `(46,44)` |
| 角色 HitTest 尾 `0x007FEC6F` | `BE23F4→BE23FC`（q） |
| 宠物 HT `801214/8013A3` | **未打** |

---

## D. −51 四维

| 侧 | 结论 |
|----|------|
| 服务端 | `Character.recalcEquipStats` `for (Item item : EQUIPPED)` — **含 −51**；`EquipSlot.PENDANT(-17,-51)`；`addInventoryInfo` 放行 −51；`SET_EXTRA_PENDANT_SLOT` 登录已发 |
| 客户端 | `77F879 cmp eax,-51 / jge` → −51 走常规装备指针；肩饰式「跳过 −20」对 −51 **不适用** |
| AvatarLook | 仍跳过 −51（选角外观），**不影响**进图属性包 |
| 验证指令 | `@第二坠` 现打印 `−51 装备四维` + 面板 `getTotalStr/Dex/Int/Luk`；穿卸对比即可 |

（服务端 Java 小改需 **重启服务端** 才能看到新提示文案；属性逻辑本就正确。）

---

## E. 部署

| 项 | 值 |
|----|-----|
| 戳记 | `PENDANT2_REMAIN_20260726r` |
| 源 | `E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` |
| 目标 | `BeiDou-Client_1\ijl15.dll`（SHA 与 out 一致） |
| VERSION | `ijl15_pendant2_VERSION.txt` |
| `pendant2_ui` | **true**（无 BOM） |
| 备份 | `_ijl15_backup\ijl15.dll.*.bak` |

---

## F. 游戏内验证清单

1. **完全退出**所有 `BeiDou.exe`，再开 Client_1  
2. `ijl15_pendant2_VERSION.txt` / 导出串含 **`PENDANT2_REMAIN_20260726r`**  
3. 进图 → 角色装备：
   - 底图像参考图（175 窄栏，非「被拉高」）
   - 第二坠 `(104,134)`：**有 Tip / 可卸**、**无红底叠字**
   - 「宠物装备」在窗内正常位置（不压底边）
   - 打开宠物装备：面板完整在主窗旁，不被挡住左列
4. `@第二坠`：看 −51 四维；穿卸后面板 STR/DEX 等应变化  
5. 武器/主坠悬停仍正常；紧急：`pendant2_ui=false` 或 `_ijl15_backup` 回滚

---

## G. 相关

- `装备栏第二坠ONE-SHOT修复_20260726q.md` — Tip/选卸 / 角色 HitTest  
- `第二坠悬停无Tip与红底_20260726n.md` — BP23 抢格说明  
- `装备栏扩图第二坠_20260726k.md` — 曾确认无更宽底图可合并  
