# 第二吊坠稳妥重做 — BP51 / −51（2026-07-26b 续查）

## 服务端进程

- **已关闭**：`org.gms.ServerApplication` + `mvn spring-boot:run` 进程树；**8484 已释放**。
- **未再启动**（用户自行重启加载本次改动）。

## 库存实况（萧曳_Xy / id=1）

| 位置 | itemId | 名称 | 结论 |
|------|--------|------|------|
| EQUIPPED **−17** | 1122000 | 黑龙项环 | 已穿主坠 |
| EQUIP **pos 29** | 1122037 | 觉醒的冒险之心 | 仍在背包 |
| EQUIPPED **−51** | — | — | **空**（从未写入） |

→ 不是「双坠已穿但漏算属性」，而是第二件根本没进 −51。

## 根因

| 层 | 状态 | 说明 |
|----|------|------|
| `EquipSlot.PENDANT` | 已开 −17 + −51 | 服务端允许双槽 |
| `recalcEquipStats` | **会算 −51** | `for (Item : EQUIPPED)` 全量遍历，无槽位过滤 |
| `addInventoryInfo` | −51 已放行 | AvatarLook 仍跳过 −51（外观） |
| 客户端 UI / ijl15 | **BP51 hooks OFF** | `ForceDrawLoop` 曾致 AuthSuccess→ALL_IDLE；DllMain / post-field 钩子均默认 false |
| 穿戴包 dst | 几乎总是 **−17** | 无第二槽 HitTest → 再穿只会**替换**主坠 |

原生 `get_bodypart_from_item(112)` 在 `bAll` 时可写出 17+51，但装备栏 UI 无 BP51 格，玩家无法把第二件拖到 −51。

## 本次代码改动（待用户重启验证）

**文件**：`InventoryManipulator.equip`

- 当 `dst ∈ {-17,-117}`、islot=`Pe`、主槽已有装备、第二槽 `{-51,-151}` 为空 → **改路由到第二槽**。
- 不挂回 ForceDrawLoop；不发 `SET_EXTRA_PENDANT_SLOT(0x79)`（083 opcode 风险，ijl15 注释已写明）。

验证步骤（用户重启服务端后）：

1. 保持黑龙在 −17，双击/穿上「觉醒的冒险之心」。
2. DB/面板：应同时有 −17 与 −51；属性应叠加。
3. 装备栏 UI 可能仍**不显示**第二格图标（客户端缺口）；以属性/DB 为准。

## 客户端缺口与下一步（勿盲开 ForceDrawLoop）

- 缺：CUIEquip 画环到 BP51、`GetSlotXY(50)` 安全坐标、HitTest 落点 51。
- 下一步（若要可见可点）：仅在 **进图后** `EnsurePendant2AfterFieldEnter` 开 `GetSlotXY` + HitTest；**禁止** DllMain 开 ForceDrawLoop。
- 当前服务端改路由可先让双坠属性生效，再单独做 UI。
