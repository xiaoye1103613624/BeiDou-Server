# 护肩 1152212–16：无动作素材风险与移除（2026-07-20）

## 结论

**穿戴有高度风险（会崩 / 槽位错乱），已从角色背包移除。**

仅缺显示、不崩：否。护肩 `islot/vslot=Sh` 本身普遍只有 `info`+icon、无 stand/walk；真正风险是服务端会穿到 **-20（BP20）**，而 ijl15 `AttachShoulderSlotsFix` 仍 **HARD-OFF**，AvatarLook / 装备栏双份空壳曾被登录二分点名。

## 证据摘要

| 项 | 结果 |
|----|------|
| `01152215.img`（Live Client_1） | 仅 `info`（属性 + icon/iconRaw ARGB4444），无 default/stand/walk |
| 对照 `01152086` / `01152210` | 同样只有 `info`+icon，无动作帧 → 「无动作」对 Sh 类是常态，不是独有缺陷 |
| 服务端槽位 | `EquipSlot.SHOULDER("Sh", -20)` |
| 客户端 | `dllmain`：肩饰模块 HARD-OFF；注释称 tiny 0115 + 背包重复为嫌疑 |
| DB 现状（清理前） | 仅主号 `萧曳_Xy`(id=1) 装备栏 pos1/2 各一件 `1152215`（未穿 -20）；`萧曳` / `LoginTest` 无 |

## 已做改动

- **DB `xiaoye1`**：删除 `inventoryitems` 42303、42304（`1152215`×2）及对应 `inventoryequipment` 20004、20005。
- **未改**客户端 String / 整类 0115；未删 `01152212–16.img` 文件。
- **未 commit**。

账号当时 `loggedin=0`，离线删库安全。

## 后续建议

肩饰模块恢复并验证 -20 渲染前，勿再给角色发 1152212–16；需要外观时再移植带正确 BP 支持的完整肩，或保持仅 String/图鉴、不进库存。
