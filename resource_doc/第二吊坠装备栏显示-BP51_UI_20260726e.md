# 第二吊坠装备栏显示（−51 / BP51）— 开启 UI 2026-07-26e

> 前置：`第二吊坠装备栏显示-BP51_UI_20260726d.md`  
> 用户反馈：−51 已穿、属性生效，装备栏仍不见第二坠格。

---

## 1. 为何之前不显示

| 原因 | 说明 |
|------|------|
| **主因** | `BeiDou-Client_1\config.ini` → `[optional] pendant2_ui=false` |
| 设计意图 | 20260726d 为防登录卡死，**默认关 UI hooks**；属性/穿戴仍靠服务端 |
| DLL | 已是 SAFE 版，未缺代码：进图后才会 `EnsurePendant2AfterFieldEnter` |

`pendant2_ui=false` 时：`AttachUiHooksOnce` 直接 return → **不**挂 GetSlotXY / HitTest、**不**写 `CUIEquip+0x5E8` → 绘制循环上界仍为 50 → 装备栏无第二坠。

---

## 2. 本次已做

| 项 | 结果 |
|----|------|
| `pendant2_ui` | **false → true**（Client_1，无 UTF-8 BOM） |
| dll | **未改** — 仍为 `PENDANT2_UI_SAFE` / SHA256 `D8F023FF…387E41` / 1,266,688 B |
| ForceDrawLoop | **未动**（`0x007FEFB9` 禁止） |
| 肩饰 | 保持 ON；六戒 OFF |
| 源码模板默认 | `BeiDou-ijl15\ezorsia\config.ini` 仍 **false**（新部署需显式打开） |
| 戳记 | `ijl15_pendant2_VERSION.txt` → `PENDANT2_UI_SAFE_20260726e` |

二进制核对（BeiDou.exe）：

- `0x007FEFB9` = `0F 95 C0`（setnz）+ `add eax,32h` → 上界 50/51
- flag 确为 `lea ebx,[reg+0x5E8]`（`0x007FEE3B`）
- 第二坠坐标 `(71,68)`：主坠 BP17=`(71,134)` 正上方空位，不挡肩饰 BP20=`(71,233)`

未启动用户服务端。

---

## 3. 现在怎么开 / 是否默认开启

| 位置 | `pendant2_ui` |
|------|----------------|
| **Client_1（当前要玩的客户端）** | **已 true（本步开启）** |
| ijl15 源码模板 / 新拷贝 | 仍 **false**（需手改或按本报告开启） |
| 关闭显示 | 改回 `false` 后重开客户端即可（不必换 dll） |

---

## 4. 验证步骤（必须按序）

1. **关掉**所有 `BeiDou.exe`，再开 `BeiDou-Client_1`
2. **先测登录**：CheckPassword → AuthSuccess → ServerList → CharList → 进频道  
   - 若 AuthSuccess 后卡住 → 立刻改 `pendant2_ui=false` 重开，并回报
3. 进图打开装备栏，看主坠上方附近 **(71,68)**：应有第二坠图标；可点/拖
4. 确认肩饰 −20、主坠 −17 正常；裤/裙红底另案
5. 旁路：`@第二坠` / `@第二坠 卸下` 仍可用

---

## 5. 若 true 后仍不显示

退路（仍禁止 ForceDrawLoop）：

1. 确认 VERSION 为 `…20260726e` 且 config 确为 `pendant2_ui=true`
2. 点 (71,68) 空位测 HitTest（有 hook 时即使图标缺也可能命中 51）
3. 下一步再做「仅装备窗打开时」的自定义叠加绘制（可关），不改 `0x007FEFB9`

紧急回滚：`pendant2_ui=false`，或 `_ijl15_backup\ijl15.dll.*.bak` 覆盖 dll。

---

*报告结束。*
