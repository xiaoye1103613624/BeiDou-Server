# 北斗服务器 - 项目架构及开发手册

---

## 目录

1. [项目概览](#1-项目概览)
2. [系统架构](#2-系统架构)
3. [网络层设计](#3-网络层设计)
4. [客户端模型层](#4-客户端模型层)
5. [游戏世界层](#5-游戏世界层)
6. [脚本引擎层](#6-脚本引擎层)
7. [服务与配置层](#7-服务与配置层)
8. [Web 管理后台](#8-web-管理后台)
9. [数据持久层](#9-数据持久层)
10. [开发指南](#10-开发指南)
11. [配置参考](#11-配置参考)

---

## 1. 项目概览

### 1.1 项目简介

北斗服务器（BeiDou Server）是一套基于 **OdinMS** 开源框架二次开发的 **冒险岛（MapleStory）** 私有服务器，服务端版本对应 **GMS v83**。项目在 OdinMS 基础上进行了大量现代化改造，引入 **Spring Boot 3.2 + JDK 21** 生态，并新增了完整的 **Web 管理后台**、装备强化/进阶系统、国际化支持、系统救援等特色功能。

### 1.2 技术栈

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| **语言** | Java | 21 | 主力开发语言（虚拟线程、Record、Pattern Matching） |
| **框架** | Spring Boot | 3.2.3 | IoC 容器 + REST API |
| **安全** | Spring Security + JWT | 3.2.3 / jjwt 0.9.1 | 认证与授权 |
| **网络** | Netty | 4.1.109 | 游戏客户端 TCP 通信 |
| **数据库** | MySQL | 8.x | 持久化存储 |
| **ORM** | MyBatis-Flex | 1.8.9 | 轻量 ORM + 代码生成 |
| **连接池** | Druid | 1.2.22 | 数据库连接池（带监控） |
| **DB迁移** | Flyway | 9.15.2 | 数据库版本管理 |
| **脚本引擎** | GraalVM JS | 23.0.4 | JavaScript 脚本执行（NPC/任务/事件） |
| **日志** | Log4j2 | Spring Boot 内置 | 结构化日志 |
| **JSON** | FastJSON2 | 2.0.47 | JSON 序列化/反序列化 |
| **API文档** | SpringDoc OpenAPI | 2.5.0 | Swagger UI |
| **Web容器** | Undertow | Spring Boot 内置 | 替代 Tomcat 的轻量 Web 容器 |
| **测试** | JUnit 5 + Mockito | 5.10.2 / 5.11.0 | 单元测试 |
| **工具** | Lombok | 1.18.30 | 消除样板代码 |

### 1.3 项目结构

```
BeiDou-Server_xy/
├── gms-server/                          # 主工程模块
│   ├── pom.xml                          # Maven 配置
│   ├── launch.bat / launch.sh           # 启动脚本
│   ├── handbook/                        # 游戏数据手册（掉落/NPC/装备等）
│   ├── scripts/                         # GraalVM JS 脚本
│   │   ├── event/                       # 事件脚本（副本/Boss）
│   │   ├── item/                        # 物品使用脚本
│   │   ├── map/                         # 地图脚本
│   │   │   ├── onFirstUserEnter/        # 首次进入地图
│   │   │   └── onUserEnter/             # 每次进入地图
│   │   └── npc/                         # NPC 对话脚本
│   ├── src/main/java/org/gms/
│   │   ├── ServerApplication.java       # Spring Boot 入口
│   │   ├── aop/                         # AOP 切面（认证/限流）
│   │   ├── client/                      # 客户端模型层
│   │   ├── config/                      # 配置类
│   │   ├── constants/                   # 常量定义
│   │   ├── controller/                  # REST 控制器
│   │   ├── dao/                         # 数据访问层
│   │   │   ├── entity/                  # 数据库实体（MyBatis-Flex 生成）
│   │   │   └── mapper/                  # Mapper 接口
│   │   ├── exception/                   # 异常定义
│   │   ├── manager/                     # 管理器
│   │   ├── model/                       # DTO / POJO
│   │   ├── net/                         # 网络层（Netty）
│   │   ├── property/                    # 配置属性
│   │   ├── provider/                    # WZ 数据提供者
│   │   ├── scripting/                   # 脚本引擎封装
│   │   ├── server/                      # 游戏逻辑层
│   │   ├── service/                     # 业务服务层
│   │   └── util/                        # 工具类
│   └── src/main/resources/
│       ├── application.yml              # 主配置文件
│       └── i18n/                        # 国际化资源
├── resource_doc/                        # 本文档目录
└── .gitignore
```

### 1.4 启动方式

```bash
# IDE 中直接运行 ServerApplication.main()
# 或命令行启动
java -jar BeiDou.jar --spring.config.location=application.yml
```

**启动流程：**

1. `ServerApplication.main()` 被调用
2. `initDb()` 解析 YML 配置，自动创建数据库（如不存在）
3. `SpringApplication.run()` 启动 Spring Boot 容器
4. `ServerConfig` 的 `CommandLineRunner` 执行游戏服务初始化：
   - 加载 WZ 游戏数据（地图/怪物/技能/NPC等）
   - 初始化世界（World）和频道（Channel）
   - 启动 Netty 登录服务器（`LoginServer`）和频道服务器（`ChannelServer`）
   - 启动定时任务（`TimerManager`）

### 1.5 端口说明

| 端口 | 用途 |
|------|------|
| `8484` | 游戏客户端登录端口（Netty TCP） |
| `7575~7575+N` | 频道服务器端口（每个频道一个独立端口） |
| `8686` | Web 管理后台端口（Spring Boot Undertow HTTP） |

---

## 2. 系统架构

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        客户端 (MapleStory Client)                    │
│                     TCP 连接 (端口 8484 / 7575+)                      │
└──────────┬──────────────────────────────────────────────┬───────────┘
           │                                              │
     ┌─────▼──────────┐                        ┌─────────▼──────────┐
     │  LoginServer   │                        │  ChannelServer x N │
     │  (Netty TCP)   │                        │  (Netty TCP)       │
     └─────┬──────────┘                        └─────────┬──────────┘
           │                                              │
     ┌─────▼──────────────────────────────────────────────▼──────────┐
     │                     PacketProcessor                           │
     │           (登录阶段) handlers/login/  +  (游戏阶段) handlers/    │
     └─────┬─────────────────────────────────────────────────────────┘
           │
     ┌─────▼──────────────────────────────────────────────────────────┐
     │                        游戏逻辑层                                │
     │  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌────────────────────┐   │
     │  │  Maps   │ │ Events  │ │  Quest   │ │  Shop/Trade/Storage│   │
     │  │  地图系统 │ │ 事件系统 │ │  任务系统  │ │     交易/仓库       │   │
     │  └─────────┘ └─────────┘ └──────────┘ └────────────────────┘   │
     └─────┬──────────────────────────────────────────────────────────┘
           │
     ┌─────▼──────────────────────────────────────────────────────────┐
     │                      脚本引擎层 (GraalVM JS)                     │
     │  ┌─────────────┐ ┌──────────────┐ ┌──────────────────────┐   │
     │  │ NPC 对话脚本  │ │  Event 事件脚本 │ │   Map/Item 功能脚本    │   │
     │  └─────────────┘ └──────────────┘ └──────────────────────┘   │
     └─────┬──────────────────────────────────────────────────────────┘
           │
     ┌─────▼──────────────────────────┬───────────────────────────────┐
     │         数据持久层               │        Web 管理后台            │
     │  ┌────────────┐ ┌───────────┐  │  ┌────────────────────────┐ │
     │  │ MyBatis-    │ │  Druid    │  │  │  Controller (REST API)  │ │
     │  │ Flex ORM    │ │ 连接池     │  │  │  AOP (Auth + RateLimit)  │ │
     │  └────────────┘ └───────────┘  │  │  Service                 │ │
     │  ┌────────────┐ ┌───────────┐  │  │  Spring Security (JWT)   │ │
     │  │  Flyway     │ │  MySQL    │  │  └────────────────────────┘ │
     │  │  数据库迁移   │ │  数据库    │  │                             │
     │  └────────────┘ └───────────┘  │                              │
     └────────────────────────────────┴──────────────────────────────┘
```

### 2.2 关键设计模式

| 模式 | 应用场景 | 示例 |
|------|----------|------|
| **单例模式** | 全局唯一的管理器 | `Server.getInstance()`, `TimerManager.getInstance()` |
| **工厂模式** | 创建复杂对象 | `MapFactory`, `PortalFactory`, `ReactorFactory`, `SkillFactory` |
| **策略模式** | GM 命令/封包处理/移动类型 | `Command` 接口, `PacketHandler` 接口, `LifeMovement` 体系 |
| **观察者模式** | 地图对象变化通知 | `MapObject` + `AbstractCharacterListener` |
| **模板方法** | 封包处理器/移动处理器 | `AbstractMovementPacketHandler`, `AbstractDealDamageHandler` |
| **建造者模式** | 封包构造 | `Packet` 内部 `ByteBufOutPacket` |
| **装饰器模式** | WZ 数据提供者封装 | `DataProviderFactory` + `DataTool` 系列 |
| **代理模式** | 延迟加载 | `DoorObject` 代理 `Door` |

### 2.3 并发模型

| 组件 | 并发策略 | 实现 |
|------|----------|------|
| **全局服务** | `ReentrantLock` / `ReentrantReadWriteLock` | `Server` 中的 `srvLock`, `wldRLock/wldWLock`, `lgnRLock/lgnWLock` |
| **地图实例** | 逐地图独立锁 | `MapleMap.mapLock` |
| **玩家存储** | `ReentrantReadWriteLock` | `PlayerStorage` 读写分离 |
| **定时任务** | `ScheduledThreadPoolExecutor` + JMX 监控 | `TimerManager` |
| **IO 线程** | Netty `EventLoopGroup` (boss + worker) | `ChannelServer`, `LoginServer` |
| **虚拟线程** | JDK 21 `Virtual Threads` | `ThreadManager` 封包处理器执行 |

### 2.4 服务生命周期

```
启动：
  ServerApplication.main()
    └─ initDb()                          # 自动建库
    └─ SpringApplication.run()
        └─ ServerConfig.run()            # CommandLineRunner
            ├─ GameConfig.load()         # 加载游戏配置
            ├─ WZ 数据加载                # Map/NPC/Skill/Mob/Item 等
            ├─ World/Channel 初始化       # 创建世界和频道实例
            ├─ LoginServer.start()       # 启动 Netty 登录服务器
            ├─ ChannelServer.start() x N  # 启动 N 个 Netty 频道服务器
            └─ TimerManager.start()      # 启动定时任务调度

运行：
  ┌─ 网络层：持续接收/解析/派发客户端封包
  ├─ 游戏层：处理战斗/移动/交互/交易等逻辑
  ├─ 脚本层：按需执行 GraalVM JS 脚本
  └─ Web 层：处理管理后台 HTTP 请求

关闭：
  Spring Boot Shutdown Hook
    ├─ Server.shutdown()
    ├─ Channel 逐一关闭并保存玩家数据
    ├─ TimerManager.stop()
    └─ Netty EventLoopGroup 优雅关闭
```

### 2.5 核心类依赖关系（简化）

```
ServerApplication
 └─ ServerConfig (CommandLineRunner)
      └─ Server (单例 - 全局状态管理)
           ├─ World x N (世界)
           │    └─ Channel x M (频道)
           │         └─ PlayerStorage (在线玩家)
           │              └─ Character (玩家角色)
           │                   ├─ Client (Netty Channel上下文)
           │                   ├─ MapleMap (当前所在地图)
           │                   │    ├─ MapObject[] (地图上的对象)
           │                   │    ├─ Monster[] (怪物)
           │                   │    ├─ NPC[] (NPC)
           │                   │    └─ Portal[] (传送门)
           │                   ├─ Inventory (背包)
           │                   ├─ Skill[] (技能)
           │                   └─ Quest[] (任务)
           ├─ Guild / Alliance (公会/联盟)
           └─ Party / Messenger / Family (社交)
```

---

## 3. 网络层设计

### 3.1 网络层架构

```
┌────────────────────────────────────────────────────────────────────┐
│                    ChannelInitializer (Netty)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐              │
│  │ PacketDecoder │→│ PacketEncoder │→│  Server/Login│             │
│  │  (解密+解码)   │  │  (编码+加密)   │  │  ChannelHandler│            │
│  └──────────────┘  └──────────────┘  └─────────────┘              │
└────────────────────────────────────────────────────────────────────┘
```

### 3.2 封包结构（GMS v83）

```
┌─────────┬──────────┬──────────────┬────────────┬──────────┐
│  4字节   │  2字节   │   可变长度     │   2字节    │          │
│  Header │  Length  │   Payload     │   Opcode   │  Data... │
│  (IV前4) │ (数据长度) │  (加密载荷)    │  (操作码)   │ (业务数据) │
└─────────┘└──────────┘└──────────────┘└────────────┘└──────────┘
```

**服务端内部封包接口：**

```java
// 输入封包（从客户端读取）
public interface InPacket {
    byte[] getBytes();
    int readByte();
    short readShort();
    int readInt();
    long readLong();
    Point readPos();
    String readMapleAsciiString();
}

// 输出封包（发往客户端）
public interface OutPacket {
    void writeByte(byte value);
    void writeShort(int value);
    void writeInt(int value);
    void writeLong(long value);
    void writePos(Point point);
    void writeMapleAsciiString(String s);
}
```

### 3.3 加密体系

**客户端 → 服务端（解密）：**

```
PacketDecoder (Netty Inbound Handler)
  ├─ MapleCustomEncryption.decryptData()    # 自定义解密（shuffle/head变换）
  ├─ MapleAESOFB.crypt()                    # AES-OFB 解密
  └─ ByteBufInPacket                        # 封装为可读封包
```

**服务端 → 客户端（加密）：**

```
PacketEncoder (Netty Outbound Handler)
  ├─ ByteBufOutPacket                       # 封包序列化
  ├─ MapleAESOFB.crypt()                    # AES-OFB 加密
  └─ MapleCustomEncryption.encryptData()    # 自定义加密（shuffle/head变换）
```

**关键类职责：**

| 类 | 职责 |
|-----|------|
| `MapleAESOFB` | AES-OFB 模式加解密（握手阶段协商 IV） |
| `MapleCustomEncryption` | 封包头变换（GMS v83 特有的魔术字节处理） |
| `InitializationVector` | 收发方向独立 IV 管理 |
| `PacketCodec` | 封包长度帧解码器（处理 TCP 粘包/拆包） |
| `PacketDecoder` | 解密 + 构建 `ByteBufInPacket` |
| `PacketEncoder` | `ByteBufOutPacket` + 加密 |
| `GMSV83PacketProtocol` | GMS v83 协议版本实现 |

### 3.4 操作码（Opcode）分发体系

```
1. 客户端发送封包 → 2字节 Opcode 标识操作类型
2. PacketProcessor.handlers[opcode] 获取对应 PacketHandler
3. handler.validateState(client) 校验客户端状态
4. handler.handlePacket(packet, client) 执行业务逻辑
```

```java
// 接收操作码（客户端 → 服务端）— 200+ 操作码
public enum RecvOpcode {
    LOGIN_PASSWORD, CHARLIST_REQUEST, CHAR_SELECT, PLAYER_LOGGEDIN,
    MOVE_PLAYER, CLOSE_RANGE_ATTACK, RANGED_ATTACK, MAGIC_ATTACK,
    TAKE_DAMAGE, NPC_TALK, ITEM_PICKUP, CHANGE_MAP, CHANGE_CHANNEL, ...
}

// 发送操作码（服务端 → 客户端）— 200+ 操作码
public enum SendOpcode {
    LOGIN_STATUS, CHARLIST, WARP_TO_MAP, SPAWN_PLAYER,
    REMOVE_PLAYER, NPC_TALK, SHOW_STATUS_INFO, UPDATE_STATS, ...
}

// Handler 注册示例
handlers[RecvOpcode.LOGIN_PASSWORD.getValue()] = new LoginPasswordHandler();
handlers[RecvOpcode.CHAR_SELECT.getValue()] = new CharSelectedHandler();
handlers[RecvOpcode.MOVE_PLAYER.getValue()] = new MovePlayerHandler();
handlers[RecvOpcode.NPC_TALK.getValue()] = new NPCTalkHandler();
```

### 3.5 双服务器设计

**LoginServer（登录服务器，端口 8484）：**
- 职责：账号认证、角色列表、角色创建/删除、频道选择
- Handler：`org.gms.net.server.handlers.login.*`

**ChannelServer（频道服务器，端口 7575 起递增）：**
- 职责：游戏内所有操作（移动、战斗、交易、聊天等）
- Handler：`org.gms.net.server.channel.handlers.*`
- 每个 World 的每个 Channel 独立启动一个 Netty Server

**频道切换流程：**

```
1. 客户端 → LoginServer: 选择世界+频道
2. LoginServer → 客户端: 返回频道的 IP:Port
3. 客户端断开 LoginServer 连接
4. 客户端 → ChannelServer: 建立新 TCP 连接
5. ChannelServer: PlayerLoggedinHandler 加载角色数据
6. 角色进入游戏（WARP_TO_MAP → SPAWN_* 系列封包）
```

### 3.6 封包处理器基类

```java
// 标准封包处理器
public interface PacketHandler {
    void handlePacket(InPacket p, Client c);
    boolean validateState(Client c);
}

// 移动类处理器抽象基类（处理移动解析通用逻辑）
public abstract class AbstractMovementPacketHandler implements PacketHandler { }

// 伤害类处理器抽象基类（处理攻击解析通用逻辑）
public abstract class AbstractDealDamageHandler implements PacketHandler { }
```

---

## 4. 客户端模型层

### 4.1 模型层总览

```
org.gms.client
├── Client.java              # Netty 客户端会话（继承 ChannelInboundHandlerAdapter）
├── Character.java           # 玩家角色（核心模型，约3000+行）
├── Job.java                 # 职业枚举（五大职业系 + 转职路线）
├── Stat.java                # 角色属性枚举（STR/DEX/INT/LUK/HP/MP...）
├── Skill.java               # 技能模型
├── SkillFactory.java        # 技能工厂
├── BuffStat.java            # Buff 状态枚举（位掩码编码）
├── Disease.java             # 异常状态枚举（封印/眩晕/中毒...）
├── Inventory 相关            # 背包系统
├── BuddyList.java           # 好友列表
├── Family.java              # 家族系统
├── MonsterBook.java         # 怪物图鉴
├── Mount.java               # 骑宠
├── Ring.java                # 婚戒/友谊戒指
├── QuestStatus.java         # 任务状态
├── autoban/                 # 反外挂系统
└── command/                 # GM 命令系统
```

### 4.2 角色属性 (Stat)

```java
public enum Stat {
    SKIN(0x1),          // 肤色         FACE(0x2),       // 脸型
    HAIR(0x4),          // 发型         LEVEL(0x10),     // 等级
    JOB(0x20),          // 职业         STR(0x40),       // 力量
    DEX(0x80),          // 敏捷         INT(0x100),      // 智力
    LUK(0x200),         // 运气         HP(0x400),       // 生命值
    MAXHP(0x800),       // 最大生命值    MP(0x1000),      // 法力值
    MAXMP(0x2000),      // 最大法力值    AVAILABLEAP(0x4000), // 可用属性点
    AVAILABLESP(0x8000),// 可用技能点    EXP(0x10000),    // 经验值
    FAME(0x20000),      // 人气         MESO(0x40000),   // 金币
    PET(0x80008),       // 宠物         // ... 50+ 属性位
}
```

### 4.3 职业体系 (Job)

```
职业ID编码规则：百位=职业系, 十位+个位表示转职阶段

    0 - 新手 (Beginner)
    ↓
100 - 战士系    200 - 法师系    300 - 弓箭手系    400 - 飞侠系    500 - 海盗系
    ↓              ↓              ↓               ↓              ↓
110 - 剑客       210 - 火毒      310 - 猎人       410 - 刺客      510 - 拳手
120 - 勇士       220 - 冰雷      320 - 弩弓手      420 - 侠盗      520 - 火枪手
130 - 英雄       230 - 祭司      330 - 神射手      430 - 隐士      530 - 船长
                 ...
900 - 贵族 (Noblesse)
    ↓
910 - 魂骑士     920 - 炎术士     930 - 风灵使者    940 - 夜行者    950 - 奇袭者
```

### 4.4 Buff/Disease 位掩码系统

**BuffStat（增益状态）：**

```java
public enum BuffStat {
    MORPH(0x2), RECOVERY(0x4), MAPLE_WARRIOR(0x8), STANCE(0x10),
    SHARP_EYES(0x20), MANA_REFLECTION(0x40), SHADOW_CLAW(0x100),
    COMBO(0x20000000), // ... 50+ Buff 类型
}
```

**Disease（异常状态）：**

```java
public enum Disease {
    NULL(0x0), SLOW(0x1), SEDUCE(0x80), STUN(0x2000),
    POISON(0x4000), SEAL(0x8000), DARKNESS(0x10000),
    WEAKNESS(0x40000), CURSE(0x80000000), // ... 20+ 异常状态
}
```

### 4.5 会话状态

```java
public static final int LOGIN_NOTLOGGEDIN = 0;       // 未登录
public static final int LOGIN_SERVER_TRANSITION = 1; // 服务器切换中
public static final int LOGIN_LOGGEDIN = 2;          // 已登录
```

### 4.6 Character.java 核心职责（约3000行）

| 功能域 | 说明 |
|--------|------|
| **属性管理** | STR/DEX/INT/LUK/HP/MP/EXP 的属性变更、装备加成、Buff 加成 |
| **等级系统** | 经验获取、等级提升、转职判定 |
| **地图交互** | 地图切换（`changeMap`）、传送门触发、地图内对象生成/销毁 |
| **战斗系统** | 伤害计算、攻击判定、死亡处理 |
| **社交系统** | 好友（BuddyList）、家族（Family）、公会（Guild）、组队（Party） |
| **背包操作** | 物品增删改查、装备穿戴/卸下、消耗品使用 |
| **序列化** | 角色状态保存到数据库 / Cookie 形式的属性封包 |
| **脚本交互** | NPC 对话上下文、任务对话上下文 |

### 4.7 Skill 系统

```java
public class Skill {
    int id;              // 技能ID
    int level;           // 当前等级
    long expiration;     // 过期时间（限时技能）
    // 通过 SkillFactory 获取技能模板数据
    SkillEntry data = SkillFactory.getSkill(id);
    // SkillEntry 包含: 动画效果(StatEffect)、最大等级、职业要求等
}
```

---

## 5. 游戏世界层

### 5.1 地图系统架构

```
org.gms.server.maps
├── MapleMap.java              # 地图核心类（管理地图上所有对象和玩家）
├── MapFactory.java            # 地图工厂（加载 WZ 地图数据）
├── MapObject.java             # 地图对象接口
├── MapObjectType.java         # 地图对象类型枚举
├── AbstractMapObject.java     # 抽象地图对象基类
├── AnimatedMapObject.java     # 动画地图对象接口
├── AbstractAnimatedMapObject.java # 抽象动画地图对象
├── Foothold.java / FootholdTree.java # 站脚点系统（碰撞检测）
├── FieldLimit.java            # 地图限制（禁止跳跃/瞬移等）
├── Portal.java                # 传送门接口
├── PortalFactory.java         # 传送门工厂
├── Reactor.java               # 反应堆（可交互机关）
├── ReactorFactory.java        # 反应堆工厂
├── ReactorStats.java          # 反应堆状态数据
├── MapItem.java               # 地面物品
├── Summon.java                # 召唤兽
├── Mist.java                  # 持续性效果区域（毒雾/火墙等）
├── Door.java / DoorObject.java # 玩家传送门
├── PlayerShop.java            # 玩家商店
├── HiredMerchant.java         # 雇佣商人
├── MiniGame.java              # 迷你游戏
├── MiniDungeon.java           # 迷你地下城
├── MapManager.java            # 全局地图管理器
└── MapMonitor.java            # 地图监控
```

### 5.2 地图分层结构

```
MapleMap (地图容器)
├── mapid / mapName                     # 地图ID/名称
├── returnMapId / forcedReturnMap       # 返回地图/强制返回地图
├── FootholdTree                        # 站脚点树（高效碰撞检测）
├── MapObject[]                         # 地图上所有对象
│   ├── Character (玩家角色)
│   ├── Monster (怪物)
│   ├── NPC (NPC)
│   ├── Summon (召唤兽)
│   ├── Mist (效果区域)
│   ├── Reactor (反应堆)
│   ├── MapItem (掉落物品)
│   ├── DoorObject (传送门)
│   └── HiredMerchant / PlayerShop / MiniGame / MiniDungeon
├── Portal[] (传送门列表)
├── fieldLimit (地图限制)
└── mapLock (并发锁)
```

### 5.3 地图对象生命周期

```
创建：
  MapFactory.loadMap(mapId) → 从 WZ 文件加载地图数据
    → 生成 Foothold / Portal / Reactor / NPC / Monster (固定生成点)
    → MapleMap 实例缓存到 MapManager

玩家进入：
  Character.changeMap(map) → Client.sendPacket(WARP_TO_MAP)
    → MapleMap.addPlayer(character)
    → 向地图内其他玩家广播 SPAWN_PLAYER
    → 向新进入玩家广播已有对象 (SPAWN_MONSTER/SPAWN_NPC/SPAWN_REACTOR...)

玩家离开：
  MapleMap.removePlayer(character)
    → 向地图内其他玩家广播 REMOVE_PLAYER
```

### 5.4 怪物系统

```
org.gms.server.life
├── LifeFactory.java            # 生命体工厂（加载WZ怪物/NPC数据）
├── Monster.java                # 怪物实例（约1000+行核心战斗逻辑）
├── MonsterStats.java           # 怪物属性（HP/MP/EXP/攻击力/元素属性等）
├── MobSkill.java               # 怪物技能
├── MobSkillFactory.java        # 怪物技能工厂
├── MobAttackInfo.java          # 怪物攻击信息
├── MonsterDropEntry.java       # 怪物掉落条目
├── MonsterGlobalDropEntry.java # 全服通用掉落
├── Element.java                # 元素属性
├── ElementalEffectiveness.java # 元素克制关系
├── NPC.java                    # NPC 实例
├── SpawnPoint.java             # 怪物出生点（含定时刷新逻辑）
└── PlayerNPC.java              # 玩家NPC
```

**怪物战斗流程：**

```
1. 客户端发送攻击封包 (CloseRangeDamage/RangedAttack/MagicDamage)
2. Handler 解析：攻击类型、攻击目标列表、伤害值
3. 伤害校验（距离、地图、状态合法性）
4. Monster.damage() → HP -= damage
5. HP <= 0 → Monster.killBy() → 计算掉落 + 经验分配
6. 广播：MONSTER_DIE / SHOW_MONSTER_HP / DROP_ITEM_FROM_MAPOBJECT
```

### 5.5 传送门系统

```
Portal (接口)
├── GenericPortal          # 通用传送门
│   ├── START              # 起始点（角色进入地图的初始位置）
│   ├── SPAWN              # 重生点
│   ├── INVISIBLE          # 不可见传送门（剧情触发用）
│   ├── VISITABLE          # 可传送门
│   ├── COLLISION          # 碰撞检测传送门
│   ├── SCRIPT             # 脚本传送门
│   └── COLLISION_SCRIPT   # 碰撞+脚本传送门
└── MapPortal              # 特殊地图传送门
```

### 5.6 反应堆（Reactor）

```java
public class Reactor extends AbstractMapObject {
    int rid;                // 反应堆ID
    byte state;             // 当前状态（0=正常, 1=已触发, 2=已摧毁...）
    ReactorStats stats;     // 反应堆数据（来自 WZ）
}

public class ReactorStats {
    List<ReactorDropEntry>[] dropEntries;  // 每个状态的掉落表
    String[] actionScript;                 // 每个状态的触发脚本
    int[] type;                            // 每个状态的类型
}
```

### 5.7 掉落系统

```
怪物死亡 → Monster.dropFrom()
  ├─ LootManager.retrieveDrop() → 从WZ掉落表+数据库掉落表计算随机掉落
  ├─ MapItem 创建
  └─ MapleMap.spawnItemOnGround() → 广播给地图玩家

拾取：
  ItemPickupHandler → MapItem.pickup(Character)
    → 检查拾取权限（远征队分配/所有者/公共）
    → 加入角色背包
    → MapleMap.removeItem() → 广播给地图玩家
```

---

## 6. 脚本引擎层

### 6.1 脚本引擎概述

北斗服务器使用 **GraalVM JavaScript** 引擎替代 OdinMS 原生的 Rhino 引擎。

```
org.gms.scripting
├── AbstractScriptManager.java   # 脚本管理器抽象基类
├── EventScriptManager.java      # 事件脚本管理器
├── EventInstanceManager.java    # 事件实例管理器
├── NPCScriptManager.java        # NPC 对话脚本管理器
├── PortalScriptManager.java     # 传送门脚本管理器
├── QuestScriptManager.java      # 任务脚本管理器
├── ReactorScriptManager.java    # 反应堆脚本管理器
├── ItemScriptManager.java       # 物品使用脚本管理器
└── event/
    ├── EventManager.java        # 事件管理器（调度事件活动）
    └── EventInstanceManager.java # 事件实例管理（副本运行时状态）
```

### 6.2 脚本类型与目录

| 脚本类型 | 存储位置 | 触发方式 | 管理器 |
|----------|----------|----------|--------|
| **NPC 对话** | `scripts/npc/{id}.js` | 点击 NPC | `NPCScriptManager` |
| **事件脚本** | `scripts/event/{name}.js` | 活动调度/玩家进入 | `EventScriptManager`/`EventManager` |
| **地图脚本** | `scripts/map/onUserEnter/{id}.js` / `onFirstUserEnter/{id}.js` | 进入地图 | `MapScriptManager` |
| **传送门脚本** | `scripts/portal/{name}.js` | 进入传送门 | `PortalScriptManager` |
| **任务脚本** | `scripts/quest/{id}.js` | 接取/完成任务 | `QuestScriptManager` |
| **反应堆脚本** | `scripts/reactor/{id}.js` | 触发反应堆 | `ReactorScriptManager` |
| **物品脚本** | `scripts/item/{id}.js` | 使用物品 | `ItemScriptManager` |

### 6.3 NPC 对话脚本模型

```javascript
// NPC 脚本中的全局对象
var status = 0;          // 对话状态索引（状态机驱动）
var cm;                  // ConversationManager（对话管理器）

function start() {       // 脚本入口
    cm.sendSimple("你好！\\r\\n#L0#选项1#l\\r\\n#L1#选项2#l");
}

function action(mode, type, selection) {
    // mode: 1=前进, -1=后退, 0=结束时
    cm.dispose();        // 结束对话
}
```

**对话管理器 API：**

```java
// cm 对象提供以下方法：
cm.sendNext("text")              // 下一步对话
cm.sendPrev("text")              // 上一步对话
cm.sendSimple("text")            // 带选项的对话
cm.sendYesNo("text")             // 是/否对话
cm.sendGetNumber("text", def, min, max)  // 获取数字输入
cm.sendGetText("text")           // 获取文本输入

// 角色操作
cm.getPlayer()                   // 获取当前角色
cm.gainItem(id, quantity)        // 给予物品
cm.gainMeso(amount)              // 给予金币
cm.gainExp(amount)               // 给予经验
cm.teachSkill(id, level, max)    // 教授技能

// 地图/传送
cm.warp(mapId)                   // 传送玩家
cm.openNpc(npcId)                // 打开另一个NPC
cm.dispose()                     // 结束对话
cm.openShop(shopId)              // 打开商店
```

### 6.4 事件系统

**EventManager（事件管理器）：**

```java
public class EventManager {
    eventManager.schedule("onStart", args);       // 定时启动
    eventManager.startInstance(eventName, player); // 玩家进入副本
    eventManager.cancel();                         // 取消事件
}
```

**EventInstanceManager（事件实例管理器）：**

```java
public class EventInstanceManager {
    // 管理：参与玩家列表、副本专属地图、副本进度、奖励分配
    eim.registerPlayer(player);          // 注册玩家
    eim.setProperty("stage", "1");       // 设置副本变量
    eim.showClearEffect();               // 通关特效
    eim.finish();                         // 结束副本
}
```

**事件脚本示例（BossRushPQ.js 简化版）：**

```javascript
function init() {
    em.setProperty("entryCount", "0");
}

function setup() {
    var eim = em.newInstance("BossRushPQ");
    eim.setProperty("stage", "0");
    return eim;
}

function playerEntry(eim, player) {
    eim.registerPlayer(player);
    player.changeMap(970030100);
}

function monsterKilled(eim, life) {
    var stage = parseInt(eim.getProperty("stage"));
    if (stage == 0 && life.getId() == 9300012) {
        eim.setProperty("stage", "1");
        // 生成下一波怪物...
    }
}

function playerDisconnected(eim, player) { }

function allMonstersDead(eim) {
    eim.showClearEffect();
    eim.giveRewards();
    eim.finish();
}
```

### 6.5 GraalVM JS 引擎配置

```java
// 引擎上下文配置
System.setProperty("polyglot.engine.WarnInterpreterOnly", "false");

// 引擎创建时绑定 Java 对象到 JS 全局作用域
ScriptEngine engine = new ScriptEngineManager().getEngineByName("graal.js");
Bindings bindings = engine.getBindings(ScriptContext.ENGINE_SCOPE);
bindings.put("cm", conversationManager);   // NPC 对话管理器
bindings.put("em", eventManager);          // 事件管理器
bindings.put("eim", eventInstanceManager); // 事件实例管理器
bindings.put("pi", playerInteraction);    // 玩家交互管理器
```

### 6.6 关键事件一览

| 事件 | 脚本文件 | 类型 |
|------|----------|------|
| 扎昆远征队 | `ZakumBattle.js` | Boss副本 |
| 暗黑龙王 | `HorntailBattle.js` | Boss副本 |
| 品克缤 | `PinkBeanBattle.js` | Boss副本 |
| 怪物嘉年华 | `MonsterCarnival.js` / `CarnivalPQ.js` | PVP副本 |
| 废弃都市组队 | `KerningPQ.js` | 组队任务 |
| 玩具城组队 | `LudiPQ.js` | 组队任务 |
| 女神之塔 | `OrbisPQ.js` | 组队任务 |
| 罗密欧与朱丽叶 | `MagatiaPQ_*.js` | 组队任务 |
| 婚礼教堂 | `WeddingCathedral.js` | 社交 |
| 椰子比赛 | `Coconut.js` | 迷你游戏 |
| 2倍经验活动 | `2xEvent.js` | 限时活动 |
| 地区Boss | `AreaBoss*.js` | 野外Boss |

---

## 7. 服务与配置层

### 7.1 配置体系

```
org.gms.config
├── GameConfig.java            # 动态游戏配置（数据库驱动，支持运行时重载）
├── ServerConfig.java          # 服务启动配置（CommandLineRunner）
├── SpringSecurityConfig.java  # Spring Security JWT 安全配置
├── CorsConfig.java            # 跨域 CORS 配置
├── I18nConfig.java            # 国际化语言配置
├── LevelRewardManager.java    # 等级奖励配置
├── NewbieGiftManager.java     # 新手礼包配置
├── EquipEnhanceManager.java   # 装备强化配置
└── EquipAdvanceManager.java   # 装备进阶配置
```

**GameConfig（动态配置系统）：**

```java
public class GameConfig {
    int maxChannels = GameConfig.getServerInt("max_channel_size");
    int expRate = GameConfig.getWorldInt(worldId, "exp_rate");
    GameConfig.reload(); // 配置热重载（GM 命令触发）
}
```

**EquipEnhanceManager（装备强化安全叠加）：**

```java
// 安全叠加装备属性值，钳制到 [0, Short.MAX_VALUE]
// 解决 GraalVM JS 中 short 溢出导致负属性使客户端闪退的问题
public static short safeAddStat(short current, int add) {
    if (add <= 0) return current;
    int result = current + add;
    if (result > Short.MAX_VALUE) return Short.MAX_VALUE;
    return (short) result;
}
```

**EquipAdvanceManager（装备进阶）：**
- 多阶段进阶路线（EquipAdvanceRoute）
- 进阶费用配置（EquipAdvanceCost）
- 进阶等级段位（EquipAdvanceStage）

### 7.2 AOP 切面体系

```
org.gms.aop
├── AuthTokenFilter.java      # JWT 认证过滤器（所有请求拦截）
├── AuthEntryPointJwt.java    # 未认证处理（返回 401）
└── ServerFilter.java         # 服务级过滤器（限流 + IP/MAC 封禁）
```

**认证流程：**

```
HTTP 请求 → ServerFilter (限流检查)
          → AuthTokenFilter (JWT 验证)
            ├─ /auth/** 路径：放行
            ├─ /swagger-ui/** 路径：放行
            ├─ 有效 Token → 设置 SecurityContext → 放行
            └─ 无效/过期 Token → AuthEntryPointJwt → 401 Unauthorized
```

### 7.3 异常处理

```
org.gms.exception
├── BizException.java               # 业务异常
├── GlobalExceptionHandler.java     # 全局异常处理器（@ControllerAdvice）
├── AccountException.java           # 账号异常
├── CharacterException.java         # 角色异常
├── InventoryException.java         # 背包异常
├── ItemException.java              # 物品异常
├── SkillException.java             # 技能异常
└── ScriptException.java            # 脚本异常
```

### 7.4 国际化（i18n）

```java
// I18nConfig: 根据 gms.service.language 配置加载语言资源
// 支持：zh-CN（简体中文）、en-US（英文）
// 资源文件：i18n/messages_zh_CN.properties

I18nUtil.getMessage("job.name.100");  // "战士" 或 "Warrior"
```

### 7.5 Spring Security 配置

```java
@Configuration
@EnableWebSecurity
public class SpringSecurityConfig {
    // - 禁用 CSRF（前后端分离）
    // - 无状态 Session（STATELESS）
    // - /auth/** 公开访问
    // - 其他接口需要 JWT 认证
}
```

### 7.6 Service 层

```
org.gms.service
├── AccountService.java        # 账号管理（注册/登录/封禁/密码加密）
├── CharacterService.java      # 角色管理（创建/删除/属性修改/IP封禁）
├── NpcService.java            # NPC 服务
├── GachaponService.java       # 转蛋机服务
├── ShopService.java           # 商店服务
├── NoteService.java           # 纸条/邮件服务
├── FamilyService.java         # 家族服务
├── NewYearCardService.java    # 新年贺卡服务
├── NameChangeService.java     # 改名服务
├── WorldTransferService.java  # 转区服务
├── NxCodeService.java         # 兑换码服务
├── NxCouponService.java       # 点券发放服务
├── HpMpAlertService.java      # HP/MP 警报服务
└── AutobanConfigService.java  # 自动封禁配置服务
```

**AccountService 密码安全：**

```java
// 注册时加密密码
String encrypted = accountService.encryptPassword(rawPassword);
// → bcrypt_migration 启用时: BCrypt.hashpw(password, BCrypt.gensalt(12))
// → 否则: SHA-512 Hash

// 登录时验证密码（兼容多种历史格式）
boolean valid = accountService.checkPassword(rawPassword, account);
// 支持：BCrypt / 明文 / SHA-1 / SHA-512
```

---

## 8. Web 管理后台

### 8.1 后台概述

内置的 **Spring Boot REST API** 管理后台，Swagger UI + JWT 无状态认证。

- 端口：`8686`
- Swagger UI：`http://localhost:8686/swagger-ui/index.html`
- OpenAPI JSON：`http://localhost:8686/v3/api-docs`

### 8.2 Controller 层（20 个控制器）

```
org.gms.controller
├── AuthController.java              # 登录/注册
├── AccountController.java           # 账号管理
├── CharacterController.java         # 角色管理
├── GachaponController.java          # 转蛋机管理
├── ShopController.java              # 商店管理
├── NoteController.java              # 纸条/邮件管理
├── NpcController.java               # NPC 管理
├── ConfigController.java            # 配置管理
├── CommandController.java           # GM命令接口
├── FileController.java              # 文件上传/下载
├── LangController.java              # 国际化资源管理
├── LogController.java               # 日志查询
├── MonitorController.java           # 服务器监控
├── OnlineController.java            # 在线玩家查询
├── RankController.java              # 排行榜
├── ReportController.java            # 举报管理
├── ServerController.java            # 服务器状态管理
├── SkillController.java             # 技能管理
├── TaskController.java              # 任务调度
└── TestController.java              # 测试接口
```

### 8.3 统一响应格式

```java
{ "code": 200, "message": "success", "data": { ... } }
```

### 8.4 认证接口

| 方法 | 路径 | 说明 | 是否需要Token |
|------|------|------|:---:|
| POST | `/auth/login` | 用户登录，返回 JWT Token | ❌ |
| POST | `/auth/register` | 用户注册 | ❌ |
| GET | `/auth/refresh` | 刷新 Token | ✅ |

### 8.5 JWT Token 结构

```java
JwtUtils.generateJwtToken(username)
// → Header: { "alg": "HS256" }
// → Payload: { "sub": "admin", "iat": 1234567890, "exp": 1234569690 }
// → Signature: HMAC-SHA256(Header.Payload, secret)

JwtUtils.validateJwtToken(token)  // → 检查签名、过期时间
```

### 8.6 跨域配置

```java
// CorsConfig: 支持前端跨域请求，允许的 Origin 在 application.yml 配置: app.vue
```

---

## 9. 数据持久层

### 9.1 ORM 框架：MyBatis-Flex

使用 **MyBatis-Flex 1.8.9**，提供简洁的 API 和高性能查询。

```java
// Mapper 接口（自动生成）
@Mapper
public interface AccountsMapper extends BaseMapper<AccountsDO> {
    @Select("SELECT * FROM accounts WHERE name = #{name}")
    AccountsDO selectByName(@Param("name") String name);
}

// Service 层使用
AccountsDO account = accountsMapper.selectOneByCondition(
    new QueryWrapper<AccountsDO>().eq("name", "admin")
);
accountsMapper.insert(account);
accountsMapper.update(account);
```

**实体类（自动生成，约90+个）：**

```java
@TableName("accounts")
@Data
public class AccountsDO {
    @Id
    private Integer id;
    private String name;
    private String password;
    private String email;
    private Date birthday;
    private Boolean banned;
    private String macs;
    // ...
}
```

### 9.2 数据库表分类（85+ 张表）

| 分类 | 核心表 | 数量 |
|------|--------|:--:|
| **账号** | accounts, hwidaccounts, ipbans, macbans, hwidbans, macfilters | 6 |
| **角色** | characters, skills, keymap, quickslotkeymapped, skillmacros, monsterbook | 6 |
| **背包** | inventoryitems, inventoryequipment, inventorymerchant, storages | 4 |
| **社交** | buddies, guilds, alliance, allianceguilds, families, marriages, rings | 7 |
| **任务** | queststatus, questprogress, questrequirements, questactions | 4 |
| **掉落** | drop_data, drop_data_global, reactordrops | 3 |
| **商店** | shops, shopitems, gachapon_reward, gachapon_reward_pool | 4 |
| **功能** | notes, dueypackages, dueyitems, newyear, worldtransfers, namechanges | 20+ |
| **配置** | game_config, extend_value, lang_resources | 3 |
| **装备强化** | equip_enhance_config, equip_enhance_cost, equip_enhance_level | 3 |
| **装备进阶** | equip_advance_route, equip_advance_cost, equip_advance_stage | 3 |
| **日志** | famelog, bosslog_daily, bosslog_weekly, eventstats | 4 |
| **NPC** | playernpcs, playernpcs_equip, playernpcs_field, plife | 4 |

### 9.3 数据库连接池：Druid

```yaml
# application.yml
mybatis-flex:
  datasource:
    mysql:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/beidou_dev_v1
```

内置监控页面：`http://localhost:8686/druid/`（SQL 执行统计、连接池状态、慢 SQL 告警）

### 9.4 数据库迁移：Flyway

```
src/main/resources/db/migration/
├── V1__init_schema.sql       # 初始建表
├── V2__add_extend_value.sql  # 扩展值表
├── V3__equip_system.sql      # 装备强化/进阶表
└── ...
```

启动时自动执行未应用的迁移脚本，版本记录在 `flyway_schema_history` 表。

### 9.5 数据库初始化

```java
// ServerApplication.initDb()
// Spring Boot 启动前自动检测数据库是否存在，不存在则创建
// 流程：解析JDBC URL → 连接MySQL → CREATE DATABASE IF NOT EXISTS → Flyway迁移
```

### 9.6 DAO 层结构

```
org.gms.dao
├── entity/   (90+ 个实体类)     → 对应数据库表，自动生成
└── mapper/   (90+ 个Mapper接口)  → 继承BaseMapper，自动生成
```

这些文件由 **MyBatis-Flex Code Generator** 自动生成，不应手动修改。

---

## 10. 开发指南

### 10.1 环境搭建

| 软件 | 版本 | 说明 |
|------|------|------|
| JDK | 21 | 需支持虚拟线程 (Virtual Threads) |
| MySQL | 8.0+ | utf8mb4 字符集 |
| Maven | 3.8+ | 项目构建 |
| IDE | IntelliJ IDEA 2023+ | 推荐使用 |

```bash
# 1. 克隆项目后配置数据库连接
# 编辑 gms-server/src/main/resources/application.yml
# 修改 mybatis-flex.datasource.mysql.url/username/password

# 2. IDEA 导入: File → Open → 选择 gms-server/pom.xml

# 3. 安装 Lombok 插件: Settings → Plugins → 搜索 Lombok → Install

# 4. 启动: 运行 org.gms.ServerApplication.main()
```

### 10.2 代码规范

遵循 **阿里巴巴 Java 开发手册（泰山版）**：
- **命名**：类名 UpperCamelCase，方法名/变量名 lowerCamelCase，常量 UPPER_SNAKE_CASE
- **异常**：不捕获后忽略，使用 try-with-resources 管理资源
- **集合**：初始化指定合理容量，返回空集合而非 null
- **并发**：明确标注线程安全策略，使用合适的锁粒度

**JDK 21 特性使用：**

```java
// Record（不可变数据载体）
public record Pair<L, R>(L left, R right) {}

// Pattern Matching for switch
switch (obj) {
    case Integer i -> processInt(i);
    case String s  -> processString(s);
    default        -> processOther(obj);
}

// 虚拟线程
Thread.ofVirtual().start(() -> { /* 轻量级并发任务 */ });
```

**Sonar 规则规避：** 始终使用 try-with-resources、Optional 判空、日志 `{}` 占位符、提取魔法值为常量。

### 10.3 添加新功能示例

**添加封包处理器：**

```java
// 1. RecvOpcode 中添加操作码
MY_NEW_FEATURE(0x1XX),

// 2. 创建 Handler
public class MyNewFeatureHandler implements PacketHandler {
    @Override
    public void handlePacket(InPacket p, Client c) {
        int param1 = p.readInt();
        // 业务逻辑
    }
    @Override
    public boolean validateState(Client c) {
        return c.getPlayer() != null;
    }
}

// 3. PacketProcessor 中注册
handlers[RecvOpcode.MY_NEW_FEATURE.getValue()] = new MyNewFeatureHandler();
```

**添加 REST API：**

```java
@Tag(name = "我的功能")
@RestController
@RequestMapping("/my-feature")
public class MyFeatureController {

    @Operation(summary = "获取数据")
    @GetMapping("/{id}")
    public Result<MyFeatureDTO> get(@PathVariable Long id) {
        return Result.success(myFeatureService.getById(id));
    }

    @Operation(summary = "创建数据")
    @PostMapping
    public Result<Void> create(@Valid @RequestBody MyFeatureRequestDTO request) {
        myFeatureService.create(request);
        return Result.success();
    }
}
```

**添加 NPC 脚本：**

```javascript
// 文件: scripts/npc/{npcId}.js
var status = 0;

function start() {
    status = -1;
    action(1, 0, 0);
}

function action(mode, type, selection) {
    if (mode == -1) { cm.dispose(); return; }
    if (mode == 1) status++; else status--;

    if (status == 0) {
        cm.sendSimple("你好！\r\n#L0#传送#l\r\n#L1#商店#l");
    } else if (status == 1) {
        if (selection == 0) { cm.warp(100000000); cm.dispose(); }
        else { cm.openShop(9200000); cm.dispose(); }
    }
}
```

### 10.4 调试技巧

```bash
# IDEA VM Options
-Dspring.config.location=./application-dev.yml
-Dlogging.level.org.gms=DEBUG

# 封包日志：GM 命令 !showpackets
# Druid 监控：http://localhost:8686/druid/
# Swagger UI：http://localhost:8686/swagger-ui/index.html
```

### 10.5 日志规范

```java
@Slf4j
public class MyClass {
    public void doSomething(String param) {
        log.info("处理请求: {}", param);        // 业务日志
        log.debug("详细参数: {}", detailObject); // 调试日志
        log.warn("异常情况: {}", warning);       // 警告
        log.error("严重错误", exception);        // 错误（传入异常对象）
    }
}
```

### 10.6 测试规范

```java
// JUnit 5 + Mockito，Given-When-Then 结构
@ExtendWith(MockitoExtension.class)
class AccountServiceTest {
    @Mock private AccountsMapper accountsMapper;
    @InjectMocks private AccountService accountService;

    @Test
    void shouldReturnAccountWhenExists() {
        // Given
        AccountsDO mockAccount = new AccountsDO();
        mockAccount.setName("test");
        when(accountsMapper.selectOneByCondition(any())).thenReturn(mockAccount);

        // When
        AccountsDO result = accountService.getByName("test");

        // Then
        assertNotNull(result);
        assertEquals("test", result.getName());
    }
}
```

### 10.7 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 启动报数据库连接失败 | MySQL 未启动或配置错误 | 检查 `application.yml` 数据库配置 |
| Flyway 迁移失败 | SQL 脚本版本冲突 | 检查 `flyway_schema_history` 表 |
| 客户端连接后立即断开 | 加密协议版本不匹配 | 检查 `GMSV83PacketProtocol` |
| NPC 脚本不执行 | JS 脚本语法错误 | 查看 GraalVM 错误日志 |
| 地图加载失败 | WZ 文件不完整 | 检查 WZ 资源文件路径 |
| 端口被占用 | 上次进程未正常退出 | `netstat -ano \| findstr 8484` |

---

## 11. 配置参考

### 11.1 application.yml 完整配置

```yaml
# ========== 服务端口 ==========
server:
  port: 8686                          # Web 管理后台 HTTP 端口

# ========== 前端地址 (CORS) ==========
app:
  vue: http://localhost:8787          # 前端 Vue 项目地址（CORS 白名单）

# ========== JWT 认证 ==========
jwt:
  secret: "81a5cd39-c4a1-f8a2-1832-a32e5d85c470"
  duration: 1800000                   # Token 过期时间（ms），默认30分钟

# ========== 数据库 & ORM ==========
mybatis-flex:
  datasource:
    mysql:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/beidou_dev_v1?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: root
  global-config:
    print-banner: false

# ========== API 文档 ==========
springdoc:
  api-docs:
    enabled: false                    # 生产环境关闭 OpenAPI
  swagger-ui:
    enabled: false

# ========== Spring 配置 ==========
spring:
  flyway:
    validate-on-migrate: false        # 禁用版本校验
  servlet:
    multipart:
      max-file-size: 1MB
      max-request-size: 10MB
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss

# ========== 北斗游戏服务配置 ==========
gms:
  service:
    language: zh-CN                   # zh-CN / en-US
    rate-limit:
      enabled: false
      limit: 10                       # 每个 IP 在 duration 内最大请求数
      duration: 1000                  # 限流窗口大小（ms）
      auto-ban: false                 # 超频后是否自动封禁 IP
    wan-host: 127.0.0.1
    lan-host: 127.0.0.1
    localhost: 127.0.0.1
    login-port: 8484                  # 游戏客户端登录端口（Netty TCP）
```

### 11.2 GameConfig 数据库配置项

| 配置键 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `update_interval` | long | 100 | 主循环更新时间间隔（ms） |
| `max_channel_size` | int | 20 | 每个世界最大频道数 |
| `max_world_size` | int | 5 | 最大世界数 |
| `channel_load` | int | 200 | 频道最大玩家数 |
| `use_ranking` | boolean | true | 是否启用排行榜 |
| `bcrypt_migration` | boolean | false | 是否使用 BCrypt 密码加密 |
| `system_rescue_maperror_changeid` | int | 0 | 卡地图救援目标地图ID（0=禁用） |

**世界级配置（w0/w1 前缀）：**

| 配置键 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `w{N}.exp_rate` | int | 1 | 经验倍率 |
| `w{N}.meso_rate` | int | 1 | 金币倍率 |
| `w{N}.drop_rate` | int | 1 | 掉落倍率 |
| `w{N}.boss_drop_rate` | int | 1 | Boss 掉落倍率 |
| `w{N}.quest_rate` | int | 1 | 任务经验倍率 |
| `w{N}.travel_rate` | int | 1 | 旅行倍率 |
| `w{N}.fishing_rate` | int | 1 | 钓鱼倍率 |
| `w{N}.max_characters` | int | 6 | 每个账号最多角色数 |

### 11.3 Druid 高级配置

```yaml
druid:
  initial-size: 10              # 初始化连接数
  min-idle: 10                  # 最小空闲连接
  max-active: 100               # 最大活跃连接
  max-wait: 60000               # 获取连接最大等待时间
  time-between-eviction-runs-millis: 60000
  min-evictable-idle-time-millis: 300000
  validation-query: SELECT 1
  test-while-idle: true
  filter:
    stat:
      enabled: true             # SQL 统计
      log-slow-sql: true
      slow-sql-millis: 5000     # 慢 SQL 阈值
    wall:
      enabled: true             # SQL 防火墙
```

### 11.4 多环境部署

```bash
# 开发环境
java -jar BeiDou.jar --spring.config.location=application-dev.yml

# 测试环境
java -jar BeiDou.jar --spring.config.location=application-test.yml

# 生产环境（敏感信息通过环境变量注入）
export DB_PASSWORD=secure_password
java -jar BeiDou.jar --mybatis-flex.datasource.mysql.password=${DB_PASSWORD}
```

所有 `application.yml` 中的配置项均可通过 `-D` 或 `--` 参数覆盖。