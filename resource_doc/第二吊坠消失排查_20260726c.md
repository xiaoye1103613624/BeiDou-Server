# 第二吊坠「消失」排查 — 2026-07-26c

## 结论（一句话）

**未丢失。** 路由 −51 **无丢物 bug**；用户看到的「消失」= **已穿在 −51，装备栏无格不显示**。

## 库存实况（萧曳_Xy / id=1）

| itemId | 名称 | 位置 | inventorytype | 说明 |
|--------|------|------|---------------|------|
| 1122037 | 觉醒的冒险之心 | **EQUIPPED −51** | −1 | 第二坠路由成功，UI 不可见 |
| 1122000 | 黑龙项环 | EQUIP **pos 29** | 1 | 在装备背包，未删 |
| 1122267 | （其它项链） | EQUIPPED **−17** | −1 | 当前主坠 |

→ 行都在；无需备份恢复 / GM 补发。

## `InventoryManipulator.equip` 审阅

| 检查点 | 结果 |
|--------|------|
| 路由条件 | `dst∈{-17,-117}` + islot=`Pe` + 主槽有物 + 第二槽空 → `dst=-51/-151` |
| 重复 remove | **无**（仅 `removeSlot(src)` 一次） |
| 未 add 到 −51 | **无**（`setPosition(dst)` + `eqpdInv.addItemFromDB`） |
| 替换目标 | 第二槽空时不碰主槽；双槽都满时仍替换主槽（可点 UI） |

根因是 **客户端缺口**（BP51 hooks OFF / 无第二坠格），不是服务端删物。

## 已做（待用户重启服务端生效）

1. 路由到 −51 后 `dropMessage`：说明「已装备、栏不显示、属性生效」+ 卸下提示。
2. 玩家指令 `@第二坠` / `@pendant2`：查看 −17/−51；`@第二坠 卸下` 卸回装备背包。
3. Flyway：`V1.11.36__pendant2_command.sql`（DB 已预插入 `command_info`）。

**未**挂 ForceDrawLoop；**未**启动服务端。

## 玩家操作

1. 重启服务端加载本次改动。
2. 看第二坠：`@第二坠`
3. 取回背包：`@第二坠 卸下`（背包需有空位）
4. 属性：−51 仍计入 `recalcEquipStats`（全量遍历 EQUIPPED）
