# 第二坠经典窄栏坐标重标定 — 2026-07-26m

## 结论
无宽版 `Equip/FullBackgrnd` 可用（见 `装备栏扩图第二坠_20260726k.md`），按用户选项 2：把 BP51 / `GetSlotXY[50]` 收到**经典窄栏**可视格。

| 项 | 值 |
|----|-----|
| 坐标 | **(104, 134)** |
| 戳记 | `PENDANT2_CLASSIC_XY_20260726m` |
| 服务端 | **未改**（−51 / `SET_EXTRA_PENDANT_SLOT` 已 OK） |
| 部署 DLL | `BeiDou-Client_1\ijl15.dll` ← `BeiDou-ijl15\out\Release\ijl15.dll` |
| SHA256 | `796CDD21A37E83FE37DC10B4F13C6D56DFFDFFA3DCAA3F194ABD7275A16D5D68` |

## 为何选 (104,134)
从 `BeiDou.exe` 经典表（`BE2580` / `BE2260`）与 UI 截图对照：

- 主坠 **(71,134)**，武器绘制 **(137,134)**，中间正好空一格 **(104,134)**  
- 经典底图上该格有空槽标签「项链」，语义就是第二坠位  
- 32×32 图标与主坠/武器**相邻不重叠**（间距 33）  
- 落在 `backgrnd` 175×304 / `FullBackgrnd` 208×291 内（104+32=136）  
- **不用**用户已否决的假格：(48,132)、(104,200)；也不用飞出画布的 (336,165)

仍保留 20260726j：右侧栏 BP4/10–16 的 HitTest 对齐到 GetSlotXY（武器悬停）。

## 代码改动
`BeiDou-ijl15/ezorsia/pendant2/pendant2.cpp`：

- `GetSlotXY[50]` = `(104,134)`
- `HitTest[50]` = `(104,134)`（原生该槽曾镜像帽子 (38,35)，一并纠正）
- `BE27E0[51]` = `(104,134)`（与经典绘制一致）
- 禁止 ForceDrawLoop；进图后 `pendant2_ui` 挂 cave@7FDE8B

部署：优先 `E:\pro\BeiDou-ijl15\out\Release\ijl15.dll` → `BeiDou-Client_1\ijl15.dll`（`deploy_ijl15.bat` 已去掉「谁新用谁」误用 `ezorsia\out` 的逻辑）。

## 游戏内验证
1. 完全退出客户端；确认 `ijl15_pendant2_VERSION.txt` 含 `20260726m`，`config.ini` 中 `pendant2_ui=true`（无 UTF-8 BOM）  
2. 进图开装备栏：第二坠应在**主坠右侧「项链」空槽**，不再飞左上、不再在 (336,165) 窗外  
3. 武器图标悬停仍出信息；主坠 −17 不挡  
4. `@第二坠` 查 −51 仍可用；穿卸第二坠可点可拖  

## 残留风险
- **宠物窗**：宠物装备窗贴在角色装备右侧，可能盖住右下格；**(104,134) 在中部项链行，一般不被宠物窗挡住**，但若用户把宠物窗拖叠到项链行，可能遮挡点击。  
- **GetSlotXY BP23** 原生也是 `(104,134)`（HitTest 却在宠物窗）。宠物相关 BP 通常走宠物 UI；若极少数路径在经典循环里画 BP23，可能与第二坠叠画——若出现再另开任务错开。  
- 窄栏无独立第二坠美术时，仍借用底图「项链」空槽外观，无单独描边。
