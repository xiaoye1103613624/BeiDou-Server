# 客户端启动卡死 A/B 诊断补充 — 2026-07-27

> 承接 hang A/B 结论：**开局卡死并非仅由 ijl15 ae 引起**；需排查其它会话对 `Data\` 的写入（尤其 Cash/Consume/String）。
> 客户端：`E:\mxd_soft\2.客户端\083\beidou_client_xiaoye\BeiDou-Client_1`
> 对照：V16 `...\BeiDou-ClientV16.1\BeiDou-Client`、`_img_merge_backup`

---

## 1. 近 ~48h `Data\` 活跃写入（live `.img`，不含 bak）

| 相对路径 | 大小 | mtime | 备注 |
|----------|------|-------|------|
| `Item\Cash\0557.img` | **1398** | 2026-07-27 00:54 | 体积小，但 **MCP 可解析**（见 §2） |
| `Item\Cash\0506.img` | 11563 | 2026-07-26 20:59 | 健康；曾有 AB 还原 bak `8249`→现 `11563` |
| `Item\Consume\0204.img` | 249726 | 2026-07-27 00:55 | 健康（V16=`190544`，本端更大属定制） |
| `Item\Consume\0243.img` | 51228 | 2026-07-26 21:13 | 健康但 **小于** V16=`62784`（热键/碎片会话） |
| `String\Cash.img` | 314211 | 2026-07-27 00:56 | 今晚写；V16=`275965` |
| `String\Consume.img` | 2595702 | 2026-07-27 00:57 | 今晚写；V16=`2607003`（略小） |
| `String\Skill.img` | 873796 | 2026-07-26 18:38 | 近 48h |
| `UI\UIWindow.img` | 11877825 | 2026-07-26 19:46 | 装备 FullBackgrnd 会话 `20260726t`（§4） |

同窗口旁证（空壳痕迹，已非 live）：

| 文件 | 大小 | mtime | 含义 |
|------|------|-------|------|
| `Item\Cash\0557.img.bak` | **155** | 2026-07-27 00:44 | **真·空壳**（PKG1 头在，无有效物品树） |
| `Item\Consume\0204.img.bak` | **1526** | 2026-07-27 00:44 | 可疑小备份（与 0557.bak 同时段） |

### 其它 `<2KB` Cash/Consume（非本轮 hang 主因）

大量 `Item\Cash\05xx.img` / `Consume\02xx.img` 体积 0.9–2KB、mtime≈**2026-07-17**。该类与 V16 的 `0557.img`(1260B) 同属「稀少物品分包」，**不能单凭体积判定损坏**。本轮应优先看 **mtime≈2026-07-27 00:44–00:57** 的写入。

---

## 2. `0557.img` 对照（MCP `083-GMS` / orange-wz）

| 来源 | 大小 | SHA256(前) | 节点 |
|------|------|------------|------|
| Client_1 live | 1398 | `C4D47C78…` | `05570000` + **`05570001`**；icon/iconRaw **ARGB4444** |
| V16 | 1260 | `CF07A03A…` | 仅 `05570000`；ARGB4444 |
| `_img_merge_backup` | 1260 | 与 V16 **相同** | 同 V16 |
| Client_1 `.bak` | **155** | — | 空壳（00:44）；**已被** 00:54 live 替换 |

**结论：当前 live `0557.img` 不是空壳，无需 OVERWRITE 还原。**  
相对 V16 多一个 `05570001`（UOL 指向 `05570000` 图标），属可解析定制/补全。  
`0557.img.bak`(155B) 保留作证据，勿当 live 使用。

**本次未执行 MCP restore**（无必要）。ijl15 / BeiDou.exe 未动；检查时文件未锁。

---

## 3. 其它 hang 嫌疑（非 ijl15 ae）

按时间线，**00:44 出现空壳 bak → 00:54–00:57 一批 Cash/Consume/String 写回**，更像「其它会话 Data 事故 + 抢救」，与纯 ijl15 ae 开局挂起可分离：

1. **已抢救成功**：`0557`（155→1398）、`0204`（有 1526 bak，live 已回 249726）、`0506` AB 还原 bak 存在。
2. **仍建议冒烟**：今晚写过的 `String\Cash.img` / `String\Consume.img`（冷启动会读 String）。
3. **0243**：小于 V16，若出现 Consume 图标/CRC 异常再单独 MCP 对照。

---

## 4. `UIWindow.img` / FullBackgrnd（`20260726t`）— 顺带记录

| 项 | Client_1 | V16 |
|----|----------|-----|
| 文件大小 | **11877825** (2026-07-26 19:46) | **12240903** (2026-07-10) |
| 差值 | −363078 | — |

MCP 快照（Client_1）：

| 节点 | 尺寸 | 格式 |
|------|------|------|
| `Equip/FullBackgrnd` | **175×304** | **ARGB4444**（`20260726t` 经典窄背包写入） |
| `Item/FullBackgrnd` | 603×289 | ARGB8888 |

详见：`角色装备背包FullBackgrnd画布还原_20260726t.md`。  
**判断：不太可能是冷启动卡死主因**（UI 资源，装备会话改动）；若卡死发生在进角色选装前，可暂时降级排查优先级。

---

## 5. ijl15

**保持不动。** 本补充未要求关端写盘；当时 `BeiDou.exe` 未占用目标 `.img`。若后续要对 locked `.img` 做 MCP `save_node`，再请用户完全退出客户端。

---

## 6. 建议复测

1. **完全退出** Client_1（清 WZ 缓存）后冷启动至选角/进图。
2. 若仍卡：临时挪走/改名今晚写过的 `String\Cash.img`、`String\Consume.img`（先备份）做 A/B，或从 V16 MCP `copy_nodes`→`paste OVERWRITE`→`save_node` 单独还原对照。
3. **不要**对 `0557` 再做 V16 OVERWRITE（会丢掉 `05570001`）。
4. 禁止 `robocopy`/`Copy-Item` 直接盖 `.img` 体。

---

## 7. 摘要表（给父代理 / 用户）

| 嫌疑 | 现状 | 是否还原 |
|------|------|----------|
| `Cash\0557.img` 空壳 | live 已可解析（1398B，2 物品，ARGB4444）；bak 155B 为历史空壳 | **否** |
| `Consume\0204` / `Cash\0506` | live 体量正常；有抢救 bak | 已由先前会话处理 |
| `String\Cash` / `String\Consume` | 今晚写入，体量正常但时间重合 | 未动；复测优先 |
| `UI\UIWindow` FullBackgrnd | 20260726t 故意改；小于 V16 | 未动；非冷启动主因 |
| ijl15 ae | A/B 已排除「单独致挂」 | 未动 |
