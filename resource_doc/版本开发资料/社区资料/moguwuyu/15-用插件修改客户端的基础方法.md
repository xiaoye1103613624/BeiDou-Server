# 用插件修改客户端的基础方法

> 来源：https://moguwuyu.com/d/15
> 站点：蘑菇物语(moguwuyu.com) · Flarum 改端技术论坛

**#1楼**

这篇文章是以北斗插件为基础去写的，如果你用的是 MapleEzorsia-v2 的话那大体上是一样的，北斗可能会有一些扩充；但是如果你用的其他版本的话，那不一定会适用，请自行判断。
前面写《如何汉化客户端内存里的字符串》时已经有提到如何用插件修改内存了，这里详细介绍一下北斗插件里几个修改内存的方法。
如果你是个小白，那么打开插件的 Client.cpp 文件，找到方法
代码登录后可见
在这个方法内添加你的修改，如果你有一定的c++经验，那么你可以自行发挥。
修改数值
常用的修改数值的命令有
代码登录后可见
其中内存地址都是 0x 开头的十六进制地址，你只需要把 ida 中复制出来的内存前面加上 0x 即可。修改值的话可以是十进制也可以是0x开头的十六进制，保持风格统一，看得懂即可。
这里面要注意的是 byte、short、int、double 分别对应 1、2、4、8个字节，你必须选择对应的方法去修改，而不是乱改，否则会修改到不该改的地方。
比如有这么一段字节码（此处均为十六进制，省略0x）
B9 01
B9 10 27 00 00
假如这段字节码表示的汇编指令是
mov     ecx, 1
mov     ecx, 2710h
你要把第一行的 1 改为其他值，那你应该使用 WriteByte，如果你使用了 WriteInt 的话，就会把（包括第二行的） 01 B9 10 27 同时修改掉了，那么整个汇编指令就错乱了。
同样的，如果你要把第二行的 0x2710 修改为其他，那你应该使用 WriteInt，如果你只使用 WriteByte 对 10 处进行修改的话就会变成
B9 XX 27 00 00
但是这里是 XX 27 00 00 一起读取的，那么就会出现修改后的值不是你的预期值的问题。
然后是内存地址的问题，IDA 处最左边显示的内存是第一个字节的地址，也就是说你要修改第一行的 1 的话，应该用 IDA 的内存地址 +1
修改字符串
然后我们北斗封装了两个方法用于修改字符串，具体的请看《如何汉化客户端内存里的字符串》
代码登录后可见
这个方法是在覆盖掉源文本后，自动计算源文本和替换文本的字节数量差，然后补足0字节来彻底覆盖掉源文本。
因为程序读取字符串的字节码时，会以0字节作为终止符，如果读不到0字节就会认为这个字符串还没结束，会继续读。
所以用原始的老方法 WriteString 去修改字符串就会遇到一个问题：比如你要把 "To Alliance" 改成 "对联盟"，就会变成 "对联盟iance"，你必须要改成 "对联盟     " 后面加5个空格才行。
用ReplaceString就不会有这个问题了。
批量修改字节
方法一 填充相同的字节
代码登录后可见
这个方法一般用于批量填充 0x90，0x90表示当前内存里啥都没有，就是一个占位符而已。
相当于删掉一部分指令，而不是替换成其他内容。
方法二 填充指定字节
代码登录后可见
这里面 byteArray 的内容要替换成自己的内容。
CodeCave 用汇编指令直接写
首先我们要明白：客户端的运行，就是按内存地址依次执行指令，而汇编指令里的 jmp 语法，就是强制让流程跳到另外一个内存地址，然后继续执行。这个指令很像 C 语言里的 goto 语法。
CodeCave 方法就是利用了 jmp 的这一特性，来实现修改的。
我拿北斗插件里的真实例子来做讲解
代码登录后可见
首先我们声明一个 DWORD 类型变量，变量值设置成返回地址。
然后声明一个裸函数，asm 里面的部分我们要用汇编指令完成，这里就是我们要做的修改。
然后使用 CodeCave 方法进行打洞。
这里的逻辑就是：
在运行客户端的时候，我们声明的裸函数会注册在内存里的某个地方
然后 CodeCave 会在 0x008C4E1B 处先填充 7 个 NOP 指令也就是 0x90（7 就是第三个参数，这个操作相当于删除了这7个内存地址里的汇编指令，但是又不改变后面的内存指令。）
然后 CodeCave 会在 0x008C4E1B 填充 jmp 指令 让程序跳到我们声明的 apDetailBtn 处。程序就开始执行 apDetailBtn 里的指令。
因此，apDetailBtn 里的逻辑必须保证程序在执行完我们的指令后 jmp 到该去的地方。也就是 apDetailBtnRtn 的地址。
由于 jmp 地址 指令是 5 个字节，因此 CodeCave 的第三个参数必须 ≥ 5。
现在我们看这段 0x008C4E1B 原本的代码
这里面，红框处中间那行的参数 7Ch 是一个 byte，用十进制表示就是 124，而我想让他变成 153。但是呢，byte 的范围是 -128 ~ 127，因此用 WriteByte 方法是无法写成 153 的，即使我们写了 0x99 或者 153 进去，最终程序读出来也是 -67 而不是 153。
所以我们借助 CodeCave 来 push 99h （这里 0x99 和 99h 都是十六进制，用起来没区别，我是参照 ida 的写法写的。）
CodeCave 是怎么做的呢？
首先，在 0x008C4E1B 处替换成 jmp，让指令跳转到我们的方法 apDetailBtn 处，由于覆盖了原来的 push 144h，所以我们的方法里要把原来指令补回去，然后push 99h，接着跳回到 0x008C4E22 处，继续后面的执行。这就是 CodeCave 的作用。
那么第三个参数的 7 是如何确定的呢？因为我们要替换至少 5 个字节，这里 push 144h 其实本身就是5个字节，所以第三个参数填 5 也是可以的，我填 7 是把后面的 push 7Ch 也一起擦除了（看图中这个指令刚好对应 2 个字节），反正这东西不会用到。
在 CE 里就可以看到修改后的样子
可以看到我们写的 push 99h 被写成 99 00 00 00 四个字节。
CodeCave 不止是可以做这种简单的修改，甚至还可以修改一些判断逻辑，里面还可以直接调用插件里的其他方法或者变量等，更多玩法还请期待后面的实战内容。
SetHook 用 C++ 直接写
SetHook 是对整个方法的劫持，不像 CodeCave 要用裸函数汇编指令来编写，SetHook是用 C++ 的语法完成的，你可以篡改传给这个方法的参数，亦或是篡改这个方法的返回结果，亦或是把参数或结果存起来另作他用，甚至可以抛弃原方法另外自己写一套实现方法都是没问题的。
示例代码
代码登录后可见
首先我们要找到一个要劫持的方法比如示例里的 0x005336CA。（每个方法的第一个地址就是调用这个方法的地址，我在介绍逆向工具的时候有讲过）
在 ida 中找到这个方法，并打开伪代码界面，移到最上面看这个方法的定义。
_DWORD *__thiscall CField::ShowMobHPTag(CField *this, int *arg0, int *a3, int a4, void *a5, signed int a6) {
然后就可以开始编写 SetHook 了
先声明一个类型 Type， typedef 后的类型我一般保持和伪代码里的一致，但是在这里，示例用的 void 而不是DWORD
。
括号里的 fastcall 一般也和伪代码中的 thiscall 处保持一致，但是呢 thiscall 在调用时有一些特殊限制（可能以后在实战的时候我会讲到 thiscall 要怎么写），所以改成了 fastcall，如果不是 thiscall 就保持原来的一致即可。 然后跟着的是类型名称，再后面是参数。**注意： **凡是 thiscall 改成 fastcall 的，要在第一个参数后面加上 void
edx，不要问我为什么，我也不知道，这是我观察别人的写法总结出的。这里面第一个参数以及 edx 后面的参数和伪代码中的参数都是一一对应的，至于参数类型和参数名称，原则上应该是保持一致的，但是这个示例作者自己找出了这些参数的真实类型和名称，所以才有了现在这样的结果。（怎么找的，以后我也会在实战的时候讲，得益于N社的大意，有个偷鸡的方法）
然后声明一个静态变量 Func，这个就是后面我们要用来调用原方法（目标方法）的东西。只要修改变量名、类型名和地址即可
再来就是注册一个 Hook 方法，SetHook 的本质就是让程序原本应该调用目标方法的，变成调用我们的 Hook 方法，然后我们在 Hook 方法里调用目标方法 Func，从而实现拦截的效果。
Hook 方法的类型就是前面注册的类型 Type，名称可以随意起，参数和 Type 的保持一致，-> void 里的 void 和 typedef 后的保持一致
然后就是里面具体的代码了，我们看，示例代码里首先是调用了原方法_Field__ShowMobHPTag：接收到的参数原封不动的传给原方法，执行一遍原方法。这样做保证原来的逻辑正常执行。然后调用了一个自定义的方法 DrawBossHpNumber(nHP, nMaxHP);
这里面就是我前面提及的：拦截了方法的参数另作他用，这里就是把这个方法的 Hp MaxHp 传给 DrawBossHpNumber 使用了。
前面都是声明、注册东西，最后要记得 Memory::SetHook 执行拦截，第一个参数表示是否启用拦截，后面的照着格式改即可
再有一个例子就是 ReplacementFuncs 里的 StringPool，这就是拦截了原方法，把结果进行修改然后返回的例子。
这个例子里的
代码登录后可见
就是执行了一遍原方法，拦截下结果，然后根据传过来的 nIdx 参数对结果进行修改，然后把修改后的结果返回给调用者。从而实现的汉化。
如何汉化客户端内存里的字符串：
https://moguwuyu.com/thread-14-1-1.html

---

**#2楼**

坐等ling总

---

**#3楼**

马克！:victory:

---

**#4楼**

楼上都是大佬，膜拜！

---

**#5楼**

师傅！！！！！！！

---

**#6楼**

"这些参数的真实类型和名称"这个在IDA怎么确定呢？:lol

---

**#7楼**

BurgerKing
ida会根据上下文给出一个初步的类型判断，但是实际的还是得自己来判断。至于变量名都是a1 a2只能自己起

---

**#8楼**

666666666666

---

**#9楼**

坐等ling总

---

**#10楼**

所以本质上客户端的修改就是修改内存，插件就是ijl15 挂载这个dll 利用他更方便修改，那我看很多相同的版本蘑菇头也不一样大，是否是脱壳后直接修改蘑菇头的，大佬有CMS079的 ij15源码吗

---

**#11楼**

qq632808989
你可以理解为，修改蘑菇头修改的是 1.中前面的内容，ijl15插件修改的是1.中后面的内容，最终都是到2里执行，效果是一样的。
蘑菇头里的内容 ---> 内存
（加载蘑菇头里的数据到内存）
CPU 执行内存里的指令
079的源码github上有，我没关注没收藏过