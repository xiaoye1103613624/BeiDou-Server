# 客户端微调实战示例

> 来源：https://moguwuyu.com/d/191
> 站点：蘑菇物语(moguwuyu.com) · Flarum 改端技术论坛

**#1楼**

针对
https://moguwuyu.com/d/190
问题
需配合视频（1分40秒开始）阅读
https://www.bilibili.com/video/BV1XTPTzvE7p/
首先问题里的提示框我是有印象在 StringPool 里的，那思路就很简单了，客户端必然有一个判断方法
如果某某条件成立，则弹出一个框(Dlg)，然后显示这个文字
那我们只需要在StringPool表中找到这段文字所属的SP ID，在IDA中通过这个ID反查就可以定位到这样的条件
然后发现这样的结构
代码登录后可见
他同时满足if判断、弹窗
代码登录后可见
以及调用这段StringPool三个条件
因此像这样结构的大概率就是我们要找的，但是他又存在于多处，所以我们要把每一处这样的都试一试
调试方法也很简单，用内存修改器，将这里的的条件判断修改一下，我们可以看到内存里的指令是
代码登录后可见
这样一段，后面紧跟着的指令就是if判断成立后执行的指令，
所以我们让jle这种带判断的跳转变成必须跳转(jmp)，这样就越过了if判断，直接跳走不执行里面的弹窗指令了。
最后调试了几个地方以后，定位到0x00A12580处的字节改成EB即可实现。（这里是比较了修改指令后的字节码和原始字节码得出的结论）
最后，这样的修改只是实现了这样一个东西，完全没有考虑是否有其他功能性的影响。如果要更好的实现这个东西，还得看看整段函数是在什么情况下触发，函数又是在做什么。只有这样才能评估修改之后造成的影响，或是使用其他更好的修改方法。

---

**#2楼**

这个怎么往DLL加代码让它生效！哎
！

---

**#3楼**

ziqiming
https://moguwuyu.com/d/15-用插件修改客户端的基础方法