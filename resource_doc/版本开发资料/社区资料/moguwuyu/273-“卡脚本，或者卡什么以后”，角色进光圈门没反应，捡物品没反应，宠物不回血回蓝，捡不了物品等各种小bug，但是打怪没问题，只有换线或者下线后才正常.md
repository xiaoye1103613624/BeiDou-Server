# “卡脚本，或者卡什么以后”，角色进光圈门没反应，捡物品没反应，宠物不回血回蓝，捡不了物品等各种小bug，但是打怪没问题，只有换线或者下线后才正常

> 来源：https://moguwuyu.com/d/273
> 站点：蘑菇物语(moguwuyu.com) · Flarum 改端技术论坛

**#1楼**

代码登录后可见
}
由于这个类里没有正确的处理客户端回传的数据，导致卡门等问题，现在把这段全部换成，下面的逻辑：
代码登录后可见
QuestActionManager现在的 QuestActionManager.dispose() 只做了：普通 NPC 脚本的 NPCConversationManager.dispose() 会解锁，而Quest 脚本没有做解锁的处理，所以把这个文件替换成下面这个：
代码登录后可见
QuestScriptManager这个最严重early return / catch / dispose 机制都不统一也替换成下面这个Quest 脚本结束时会明确解锁，脚本异常时会明确解锁
脚本重复进入节流失败引擎空脚本时也会明确解锁：
代码登录后可见

---

**#2楼**

ScriptedItemHandler.java这个文件修改成你上面的，会导致报错if (org.gms.service.RandomItemBoxService.use(chr, realItemId)) {
return;
} 没有这个类

---

**#3楼**

if (org.gms.service.RandomItemBoxService.use(chr, realItemId)) {
return;
}
这一串应该是大哥加的盒子物品的功能，所以删除也不影响使用

---

**#4楼**

感谢大哥的分享