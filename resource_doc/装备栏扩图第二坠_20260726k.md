# 装备栏扩图（第二坠 336,165）— 评估与阻塞说明 2026-07-26k

## 目标
加宽 `UI/UIWindow.img` 装备窗底图，使 ijl15 戳记 `PENDANT2_LAYOUT_FIX_20260726j` 使用的原生坐标 **(336,165)** 落在真实项链格上。  
**不改** ijl15 坐标；服务端 −51 / `SET_EXTRA_PENDANT_SLOT` 已 OK。

## 结论（本轮）
**未写入任何 `.img`。** 在已知良源中 **找不到** 宽度 ≥368（建议 ≥504）的装备展开底图，禁止用网页图 / ARGB8888 乱造图标顶替。

| 项 | 结果 |
|----|------|
| orange-wz HTTP MCP `:10002` | 健康（Cursor 桥接曾报 Not connected，改用 HTTP `tools/call` 包装，等价 MCP） |
| 扩图是否已落地 | **否** |
| 坐标是否改动 | **否**（保持 336,165） |

## 现场画布尺寸（MCP 实测）

路径：`…/BeiDou-Client_1/Data/UI/UIWindow.img` → `Equip/*`

| 节点 | LIVE | V16 Data | EN | Maples / BeiDouV7.7 | 某私服北斗 UI.wz |
|------|------|----------|----|---------------------|------------------|
| `Equip/backgrnd` | 175×304 | 同左 | 同左 | 同左 | 同左 |
| `Equip/FullBackgrnd` | **208×291** | 同左 | 同左 | 同左 | 同左 |
| `Equip/pet` | 177×181 | 同左 | 同左 | 同左 | 同左 |
| `Item/FullBackgrnd`（对照，背包扩格） | 603×289 | — | — | — | 603×289 |

经典窄栏最大可视约 x≈170；`FullBackgrnd` 仅比 `backgrnd` 宽约 +33，**盖不住 x=336**。

另扫：`mxd_soft` 下全部 `UIWindow.img.xml`、BMS250 TMS `UIWindow*.xml`、V16 / EN / `_img_merge_backup` — **无** Equip 底图 width≥300。

## 原生坐标表（BeiDou.exe）

| 表 | VA | 用途 | BP51 / index50 |
|----|-----|------|----------------|
| `BE27E0` | `0x00BE27E0` | CUIEquip 槽位表（按 BP） | **[51]=(336,165)** |
| `BE2580` | `0x00BE2580` | 经典 GetSlotXY（index=BP−1） | 原生无有效第二坠格；ijl15 写入 (336,165) |
| `BE2260` | `0x00BE2260` | 经典 HitTest | 无 BP51 |

`BE27E0` 非零范围约 **(14,27)…(472,198)** → 若要对齐整张展开表，底图建议 **≥504×230**（仅第二坠格则 **≥368×197**，并在 (336,165) 画项链空槽外观）。

> 说明：`Item/FullBackgrnd` 603×289 是**物品栏**扩格美术，样式/布局不同，**不能**直接贴到 `Equip/FullBackgrnd`。

## 需要的美术（待用户提供源）

优先节点（展开旗标 / `pendant2_ui` 时客户端应吃宽图）：

1. **`UIWindow.img/Equip/FullBackgrnd`**  
   - 目标尺寸：**≥368×291**（保现高）；若要对齐整表 **≥504×291**（或更高，与宠物条布局协调）  
   - 格式：**ARGB4444**（v083 推荐；勿注入 web ARGB8888）  
   - 在客户区约 **(336,165)** 留出项链空槽视觉（与主坠 `≈(71,134)` 区分）

可选（若展开态仍读窄底）：

2. **`UIWindow.img/Equip/backgrnd`** — 仅当确认绘制路径仍用它时再加宽（当前经典 175 宽）。

来源要求：已知良源 `.img` / 官方或已验证私服包，经 **orange-wz MCP** `load → copy_nodes → paste_nodes → save_node` 合并；禁止 `Copy-Item`/`robocopy` 整包覆盖。

## 文档与相关戳记

- `装备栏错位修复_20260726j.md`：坐标已回到 (336,165)；扩图另开任务 ← **本文件**
- `第二坠位置纠正_20260726i.md`：勿再造鞋子旁/主坠旁假格
- ijl15：`PENDANT2_LAYOUT_FIX_20260726j` — **本轮未重建**

## 游戏内验证（扩图落地后）

1. 确认 `ijl15_pendant2_VERSION.txt` 含 `20260726j`，`config.ini` `[optional] pendant2_ui=true`（无 UTF-8 BOM）  
2. **完全退出**客户端再开  
3. 开装备栏：窗口右侧展开区约腰带行高度应见第二坠格；(336,165) 落在底图空槽上，图标不飞出窗外  
4. 主坠 / 武器悬停正常；`@第二坠` 仍可用  

## 下一步请用户提供

任选其一即可继续 MCP 合并：

1. **含宽版 `Equip/FullBackgrnd` 的 UIWindow.img / UI.wz 路径**（width≥368，最好≥504），或  
2. **单独 ARGB4444 PNG**（尺寸如上）+ 明确要写入的节点路径，或  
3. 若接受「整表重标定」备选方案（把第二坠改回窄栏可视格），需另开任务并改 ijl15（与本次 option1 相反）。
